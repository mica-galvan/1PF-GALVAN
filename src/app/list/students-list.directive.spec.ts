import { StudentsListDirective } from './students-list.directive';

describe('StudentsListDirective', () => {
  it('should create an instance', () => {
    //const directive = new StudentsListDirective();
    //expect(directive).toBeTruthy();
  });
});
